# 이벤트 기반 분산 트랜잭션 설계

## 목차
1. [분산 트랜잭션 문제](#1-분산-트랜잭션-문제)
2. [Step15 구현: 비동기 이벤트 처리](#2-step15-구현-비동기-이벤트-처리)
3. [도메인별 서비스 분리 시 트랜잭션 처리의 한계와 대응 방안](#3-도메인별-서비스-분리-시-트랜잭션-처리의-한계와-대응-방안)
4. [Saga 패턴 개요](#4-saga-패턴-개요)
5. [마이크로서비스 환경에서의 확장](#5-마이크로서비스-환경에서의-확장)
6. [결론](#6-결론)

---

## 1. 분산 트랜잭션 문제

### 1.1 단일 트랜잭션의 한계
```
[모놀리식: 단일 트랜잭션]
쿠폰 사용 → 재고 차감 → 포인트 차감 → 주문 생성 → 결제 완료 → 외부 API 전송

문제: 외부 API 실패 시 불필요한 전체 롤백
```

### 1.2 트랜잭션 분리 시 발생하는 문제
```
[마이크로서비스: 독립된 트랜잭션]
주문 서비스 → 성공 (커밋)
재고 서비스 → 성공 (커밋)
포인트 서비스 → 실패

문제: 이미 커밋된 트랜잭션 롤백 불가능
```

**핵심 과제:**
- 데이터 불일치 (일부만 성공)
- 중복 처리 (네트워크 재시도)
- 순서 보장 문제
- 부분 실패 시 복구 방법

---

## 2. Step15 구현: 비동기 이벤트 처리

### 2.1 결제 완료 이벤트 (비동기 처리)

**문제 상황:**
결제 트랜잭션 내에서 외부 API 전송 → 외부 API 실패 시 결제까지 롤백

**해결 방안:**
```
결제 로직 수행 → 결제 완료 → 트랜잭션 커밋
                              ↓
                      이벤트 발행 (AFTER_COMMIT)
                              ↓
                      외부 API 전송 (비동기)
```

**구현 방식:**
- `@TransactionalEventListener(AFTER_COMMIT)`: 트랜잭션 커밋 후 이벤트 발행
- `@Async`: 비동기로 외부 API 전송

**핵심 효과:**
결제 트랜잭션과 외부 API 전송 분리 → 외부 API 실패 시에도 결제 완료 상태 유지

---

## 3. 도메인별 서비스 분리 시 트랜잭션 처리의 한계와 대응 방안

### 3.1 서비스 확장 시나리오

이커머스 서비스의 성장에 따라 단일 모놀리식 아키텍처에서 도메인별로 분리된 마이크로서비스 아키텍처로 전환하는 시나리오를 가정합니다.

#### 분리 전: 모놀리식 아키텍처
```
[단일 애플리케이션 서버]
├── 주문 모듈
├── 결제 모듈
├── 쿠폰 모듈
├── 재고 모듈
└── 상품 모듈
      ↓
[단일 데이터베이스]
```

**장점:**
- 단일 트랜잭션으로 ACID 보장
- 간단한 데이터 정합성 관리
- 배포 및 운영 단순

**한계:**
- 특정 도메인 부하가 전체 시스템에 영향
- 도메인별 독립적인 확장 불가
- 장애 전파 (한 모듈 실패 → 전체 서비스 다운)
- 배포 시 전체 서비스 중단 필요

#### 분리 후: 도메인별 마이크로서비스

```
[주문 서비스]          [결제 서비스]          [쿠폰 서비스]
      ↓                     ↓                     ↓
  [주문 DB]             [결제 DB]             [쿠폰 DB]

[재고 서비스]          [상품 서비스]          [사용자 서비스]
      ↓                     ↓                     ↓
  [재고 DB]             [상품 DB]             [사용자 DB]
```

**장점:**
- 도메인별 독립적인 확장 가능
- 장애 격리 (한 서비스 장애가 다른 서비스에 영향 최소화)
- 기술 스택 독립성
- 독립적인 배포 및 개발

**새로운 도전 과제:**
- 분산 트랜잭션 문제
- 데이터 정합성 보장의 어려움
- 네트워크 지연 및 장애
- 서비스 간 통신 복잡도 증가

---

### 3.2 도메인별 분리 시 발생하는 트랜잭션 처리의 한계

#### 한계 1: 단일 트랜잭션의 불가능

**시나리오: 주문 생성 프로세스**

```java
// 모놀리식에서는 가능했던 코드
@Transactional
public void createOrder(OrderRequest request) {
    // 1. 쿠폰 사용 처리 (쿠폰 DB)
    couponService.useCoupon(request.getCouponId());

    // 2. 재고 차감 (재고 DB)
    stockService.decreaseStock(request.getProductId(), request.getQuantity());

    // 3. 포인트 차감 (사용자 DB)
    pointService.deduct(request.getUserId(), request.getPointToUse());

    // 4. 주문 생성 (주문 DB)
    Order order = orderRepository.save(createOrder(request));

    // 5. 결제 처리 (결제 DB)
    paymentService.processPayment(order.getId(), request.getPaymentInfo());

    // 모든 작업이 하나의 트랜잭션으로 묶임
    // 하나라도 실패하면 자동 롤백
}
```

**분리 후 문제:**
```
각 서비스가 독립된 DB를 사용하므로:

[쿠폰 서비스 - 쿠폰 DB]
  → 트랜잭션 1: 쿠폰 사용 완료 (커밋됨)

[재고 서비스 - 재고 DB]
  → 트랜잭션 2: 재고 차감 완료 (커밋됨)

[포인트 서비스 - 사용자 DB]
  → 트랜잭션 3: 포인트 부족으로 실패

문제: 쿠폰과 재고는 이미 커밋되어 롤백 불가능!
```

#### 한계 2: 부분 실패 처리의 복잡성

**3단계 프로세스에서 2단계 성공 후 3단계 실패 시:**

```
1. [쿠폰 서비스] 쿠폰 사용
   → DB 커밋 완료

2. [재고 서비스] 재고 차감
   → DB 커밋 완료

3. [결제 서비스] 결제 실패
   → PG사 통신 오류

어떻게 복구할 것인가?
   - 쿠폰 사용 취소 필요
   - 재고 복구 필요
   - 각 서비스에 보상 API 호출 필요
```
---

### 3.3 대응 방안

#### 방안 1: Saga 패턴을 통한 분산 트랜잭션 관리

**Choreography 방식 (이벤트 기반)**

**개념:**
- 각 서비스가 독립적으로 이벤트를 발행하고 구독하여 처리
- 중앙 제어 없이 서비스 간 협업

**처리 흐름:**
```
주문 서비스 → 이벤트 발행
  ↓
재고 서비스 → 재고 차감 후 이벤트 발행
  ↓
쿠폰 서비스 → 쿠폰 사용 후 이벤트 발행
  ↓
결제 서비스 → 결제 처리

실패 시 → 보상 이벤트 발행 → 역순으로 복구
```

**핵심 요소:**
1. **멱등성 보장**: 각 도메인 DB의 event 테이블에 처리 기록 저장하여 중복 처리 방지
2. **보상 트랜잭션**: 실패 시 완료된 작업들을 역순으로 복구 (재고 복구, 쿠폰 복구 등)

#### 방안 2: Orchestration 방식 (Saga 오케스트레이터)

**개념:**
- 중앙 오케스트레이터가 모든 트랜잭션 흐름을 제어하고 관리
- 각 서비스는 오케스트레이터의 명령에 따라 동작

**처리 흐름:**
```
오케스트레이터
  → 재고 서비스 호출 → 결과 저장
  → 쿠폰 서비스 호출 → 결과 저장
  → 결제 서비스 호출 → 결과 저장

실패 시 → 오케스트레이터가 역순으로 보상 API 호출
```

**핵심 요소:**
- 중앙 집중식 제어 및 상태 관리
- 명확한 롤백 시나리오

---

** 선택: 방안 1 (Choreography)**

**선택 이유:**
1. **단순한 비즈니스 흐름**: 주문 → 재고 → 쿠폰 → 결제의 단순한 순차 흐름
2. **서비스 독립성**: 각 도메인이 독립적으로 확장 가능

### 3.4 도메인별 분리 전략 및 우선순위

#### Phase 1: 읽기 트래픽이 많은 도메인 먼저 분리
```
[상품 서비스] - 읽기 전용, 트랜잭션 문제 최소
[사용자 서비스] - 인증/조회 위주
```

#### Phase 2: 쓰기 트래픽이 적은 도메인 분리
```
[쿠폰 서비스] - 비교적 단순한 비즈니스 로직
```

#### Phase 3: 핵심 트랜잭션 도메인 분리
```
[주문 서비스] ←→ [재고 서비스] ←→ [결제 서비스]
(Saga 패턴 적용 필수)
```

---

### 3.5 트레이드오프 및 의사결정 기준

| 항목 | 모놀리식 | 마이크로서비스 |
|-----|---------|--------------|
| 트랜잭션 보장 | 강한 일관성 (ACID) | 최종 일관성 (Eventual) |
| 데이터 정합성 | DB 레벨 보장 | 애플리케이션 레벨 보장 |
| 개발 복잡도 | 낮음 | 높음 (이벤트, 보상 로직) |
| 확장성 | 수직 확장 | 수평 확장 (도메인별) |
| 장애 격리 | 어려움 | 쉬움 |

---

## 4. Saga 패턴 개요

Saga 패턴은 분산 트랜잭션을 여러 개의 로컬 트랜잭션으로 나누고, 실패 시 보상 트랜잭션을 통해 데이터 일관성을 보장하는 패턴입니다.

### 4.1 Choreography 방식
각 서비스가 독립적으로 이벤트를 발행하고 구독하여 처리

```
주문 서비스 → [OrderCreated] → 재고 서비스
                                    ↓
                              [StockReserved] → 결제 서비스
                                                      ↓
                                                [PaymentCompleted]
```

### 4.2 핵심 요소

**1. 멱등성 보장**
- 동일한 이벤트는 여러 번 처리되어도 결과가 동일
- event 테이블에 처리 기록 저장하여 중복 처리 방지

**2. 보상 트랜잭션**
- 실패 시 완료된 작업들을 역순으로 복구
- 예: 결제 실패 → 재고 복구 → 쿠폰 복구 → 주문 취소

**3. 순서 보장**
- 이벤트 버전 관리로 처리 순서 보장
- 순서가 맞지 않으면 대기열에 보관 후 순차 처리

---

## 5. 마이크로서비스 환경에서의 확장

### 5.1 메시지 브로커 사용 (Kafka, RabbitMQ)

**현재 상황:**
- Spring의 ApplicationEventPublisher 사용 (모놀리식 환경)
- 서비스 분리 시 메시지 브로커 도입 필요

**Kafka Producer (이벤트 발행 측 - 주문 서비스):**
- KafkaTemplate을 사용하여 이벤트 발행
- orderId를 메시지 키로 사용 → 동일 주문 이벤트는 동일 파티션으로 전송
- 파티션 내에서 순서 보장

**Kafka Consumer (이벤트 구독 측 - 재고 서비스):**
- `@KafkaListener`로 특정 토픽 구독 (예: "order-events")
- Consumer Group 지정 (예: "stock-service")
- 이벤트 수신 후 처리:
  1. 멱등성 체크 (event_processing 테이블 확인)
  2. 중복 이벤트면 즉시 종료
  3. 미처리 이벤트면 비즈니스 로직 수행
  4. 처리 기록 저장

**주요 특징:**
- 서비스 간 비동기 통신
- 높은 처리량 및 확장성
- 메시지 영속성 보장
- 다수의 Consumer 지원

---

## 6. 결론

### 현재 구현 상태
- 외부 API 전송을 비동기 이벤트로 분리
- @TransactionalEventListener 사용
- 트랜잭션 커밋 후 이벤트 발행

### 향후 개선 방향
1. **멱등성 보장 메커니즘 추가**
   - orderId 기반 중복 처리 방지

2. **보상 트랜잭션 구현**
   - 실패 이벤트 정의
   - 보상 리스너 구현
   - 재고/포인트 복구 로직

3. **마이크로서비스 전환 대비**
   - 메시지 브로커 도입 검토
   - Saga 패턴 적용
   - 분산 추적(Distributed Tracing) 도입

